# 深度学习面试题

> 基于《深度学习详细知识文档》提取生成，涵盖概念、激活函数、损失函数、梯度下降、过拟合、优化器、反向传播、权重初始化、综合场景及 PyTorch 框架。

---

## 一、基础概念

**Q1.** 什么是深度学习？它和传统机器学习的本质区别是什么？

**Q2.** 描述一个神经元的工作流程。公式 $z = \sum (x_i \cdot w_i) + b$ 中，$w_i$ 和 $b$ 分别代表什么含义？

**Q3.** 为什么必须在加权求和之后引入**非线性激活函数**？如果所有层都是线性变换，深层网络会退化成什么？

**Q4.** 以"识别猫狗图片"为例，说明深度学习逐层提取特征的过程（从边缘 → 形状 → 整体）。

---

## 二、激活函数

**Q5.** Softmax 的公式是什么？它为什么适合**多分类**场景？它的输出有什么约束条件？

**Q6.** Softmax 公式中为什么使用 $e^x$ 而不是 $2^x$ 或 $x^3$？从反向传播角度说明。

**Q7.** 如果一张图片中**同时有猫和狗**，Softmax 还适用吗？应该用什么替代方案？请从业务场景和数学机制两个角度解释。

**Q8.** 对比 ReLU、Sigmoid、Tanh 三个激活函数的公式、输出范围、优缺点和适用场景。

| 激活函数 | 公式 | 输出范围 | 主要缺点 | 适用场景 |
|---------|------|---------|---------|---------|
| ReLU | | | | |
| Sigmoid | | | | |
| Tanh | | | | |

**Q9.** ReLU 的"神经元死亡"现象是什么？为什么会发生？如何解决？

**Q10.** 为什么 Sigmoid 在深层网络（超过 5 层）中容易导致梯度消失？结合其最大梯度值说明。

**Q11.** Tanh 相比 Sigmoid 有什么改进？为什么现代深度网络仍然很少用它？

---

## 三、损失函数

**Q12.** 什么是损失函数？为什么训练集和测试集上的损失**都越小**，说明模型越好？

**Q13.** 均方误差（MSE）的公式和梯度特征是什么？它对异常值极其敏感的根本原因是什么？

**Q14.** 数据预处理阶段已经处理了异常值，为什么训练阶段还会存在让模型"头疼"的样本？请举例说明三类隐性困难样本。

**Q15.** 对比 L1 Loss（MAE）、MSE Loss 和 Smooth L1 Loss：

| 损失函数 | 公式 | 梯度特点 | 优点 | 缺点 | 典型应用 |
|---------|------|---------|------|------|---------|
| MAE | | | | | |
| MSE | | | | | |
| Smooth L1 | | | | | |

**Q16.** Smooth L1 Loss 是如何在"误差小像 MSE、误差大像 L1"之间切换的？阈值一般设为多少？

**Q17.** 工业界解决隐性困难样本的四种方法分别是什么？各自针对哪类问题？

---

## 四、梯度与梯度下降

**Q18.** 什么是梯度？单变量函数和多变量函数的梯度有什么不同？

**Q19.** 为什么参数更新是沿着梯度的**负方向**而非正方向？

**Q20.** 在高维非凸损失函数中，为什么找到**局部最优解**往往就够用了？"鞍点"和"局部最优"哪个才是训练中真正卡住的原因？

**Q21.** 为什么一个损失值极小（尖锐的全局最优）反而可能是过拟合的信号？平坦的局部最优为什么更有价值？

**Q22.** 对比三种梯度下降方法：BGD、SGD、Mini-Batch GD，说明各自的优缺点。

| 方法 | 每次迭代使用的样本量 | 优点 | 缺点 |
|------|-------------------|------|------|
| BGD（批量梯度下降） | | | |
| SGD（随机梯度下降） | | | |
| Mini-Batch GD（小批量梯度下降） | | | |

**Q23.** 不做梯度下降优化会怎样？请用权重更新公式 $W_{new} = W_{old} - \eta \cdot \frac{\partial L}{\partial W}$ 串起来解释整个优化流程。

**Q24.** 为什么 Mini-Batch GD 中的噪声反而有利于训练？

---

## 五、过拟合与欠拟合

**Q25.** 分别解释**过拟合**、**欠拟合**、**正好拟合**的表现和判断标准。

**Q26.** 解决过拟合的方法有哪些？请按以下三个方向分别列举：
- 数据层面
- 正则化层面
- 训练策略层面

**Q27.** L1 正则化和 L2 正则化的数学差异是什么？为什么 L1 会让权重**等于 0**，而 L2 只让权重**趋于 0**？

**Q28.** Dropout 的工作原理是什么？为什么说它相当于"训练了无数个小模型做集成"？

**Q29.** 早停（Early Stopping）防止过拟合的原理是什么？

---

## 六、学习率与优化器

**Q30.** 学习率过大或过小分别会导致什么问题？工业界通常将学习率设在什么范围？

**Q31.** 为什么要对学习率做动态优化？列举三类学习率衰减策略，并说明各自适用场景。

| 衰减策略 | 规则 | 适用场景 |
|---------|------|---------|
| StepLR（等间隔） | | |
| MultiStepLR（指定间隔） | | |
| ExponentialLR（指数衰减） | | |

**Q32.** Warmup（预热）是什么？为什么在 Transformer 类模型中是标配？

**Q33.** 解释**指数加权移动平均（EMA）**公式 $v_t = \beta v_{t-1} + (1-\beta)\theta_t$ 中每个符号的含义。为什么叫"指数加权"？

**Q34.** 当 $\beta = 0.9$ 时，当前时刻、上一时刻、上上时刻的梯度权重分别是多少？验证权重呈指数级递减。

**Q35.** 动量法（Momentum）相比普通 SGD 解决了什么问题？

**Q36.** 对比以下优化器，说明各自的核心思想和改进点：

| 优化器 | 核心思想 | 解决的问题 |
|--------|---------|-----------|
| Momentum | | |
| AdaGrad | | |
| RMSProp | | |
| Adam（最常用） | | |

**Q37.** Adam 为什么需要**偏差校正**？如果没有偏差校正，训练初期会发生什么？

**Q38.** 优化器在代码中的作用是什么？为什么每次 backward 之后需要手动调用 `optimizer.zero_grad()`？

---

## 七、反向传播

**Q39.** 反向传播（Backpropagation）是什么？它和"梯度下降"的职责分别是什么？

**Q40.** 既然可以直接从损失函数计算梯度 $\frac{\partial L}{\partial W}$，为什么还需要反向传播？

**Q41.** 用文字描述一次完整的训练迭代过程：输入 → 前向传播 → 计算损失 → 反向传播 → 参数更新。

---

## 八、权重初始化

**Q42.** 为什么权重不能全部初始化为 0？偏置 $b$ 为什么可以初始化为 0？

**Q43.** Xavier 初始化和 He/Kaiming 初始化的核心思想和适用场景有什么不同？

| 初始化方法 | 核心思想 | 公式 | 适用激活函数 |
|-----------|---------|------|------------|
| Xavier/Glorot | | | |
| He/Kaiming | | | |

**Q44.** He 初始化公式 $W \sim \mathrm{Normal}(0, \sqrt{\frac{2}{n_{in}}})$ 中，为什么要乘以 $\sqrt{2}$？

**Q45.** 正交初始化适用于什么场景？为什么它能缓解梯度消失/爆炸？

**Q46.** 预训练初始化在什么场景下使用？它解决了什么问题？

---

## 九、综合场景题

**Q47.** 你在训练一个 50 层的 CNN 图像分类模型，发现：
- 训练集准确率 99%，验证集准确率只有 72%
- 训练损失在持续下降，验证损失在第 15 个 epoch 后开始上升

请分析可能的原因，并给出至少 4 种改进方案。

**Q48.** 训练时发现 loss 一直不下降（loss 曲线是一条水平的直线），可能的原因有哪些？如何逐一排查？

**Q49.** 训练时发现 loss 出现 NaN，可能的原因有哪些？如何解决？

**Q50.** 一个实际业务场景：你要做商品图片的**多标签分类**（一件商品可能同时属于"上衣""条纹""春季新款"），输出层应该选什么激活函数？损失函数选什么？为什么？

**Q51.** 你在训练时发现 GPU 利用率很低（<30%），可能的原因有哪些？如何优化？

---

## 十、PyTorch 框架

**Q52.** PyTorch 中张量形状 `(batch_size, feature_dim)` 的约定是什么？`(32, 128)` 表示什么含义？

**Q53.** PyTorch 训练循环的标准五步是什么？（提示：forward → loss → zero_grad → backward → step）

**Q54.** `model.train()` 和 `model.eval()` 的区别是什么？哪些层会受到这个模式切换的影响？

**Q55.** PyTorch 中 `with torch.no_grad():` 的作用是什么？在什么阶段必须使用它？

---

## 十一、前向传播与神经网络训练流程

**Q56.** 前向传播的完整过程是什么？从输入层到输出层，每层做了哪两件事？

**Q57.** 画出一张完整的深度神经网络训练流程图，标注每个步骤的输入和输出。

**Q58.** 深度学习模型是如如何"自动"提取特征的？卷积核的权重是如何从随机变得有意义的？

**Q59.** 为什么说深度学习"端到端学习"？这和传统机器学习的人工特征工程有什么区别？

---

> **共计 59 题**，覆盖深度学习核心知识点。建议按模块分阶段复习，每道题先尝试独立作答，再对照原文查漏补缺。
